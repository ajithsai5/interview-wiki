# Log

Append-only timeline. Each entry: `## [YYYY-MM-DD] <type> | description`.
Recent activity: `grep "^## \[" log.md | tail -5`

## [2026-05-29] build | Scaffolded the wiki — schema (CLAUDE.md), index, README, and two example pages: [[arrays-and-hashing]] and [[bias-variance]].
## [2026-05-29] build | Added the wiki viewer app (app/) from the Claude Design handoff — warm-paper editorial reader with sidebar nav, search, status tracking + spaced-review queue. build.py generates app/notes-data.js from the .md notes; run.bat builds + opens it.
## [2026-05-29] build | Bundled the design's 46 seed sample notes into app/content/ (merged, user notes win on id clash) and fixed the review-queue "20600d overdue" bug (seeded-but-unreviewed notes now show "ready").
## [2026-05-29] ingest | Authored the Arrays interview-prep track: 14 pages in dsa/arrays/ ([[arrays-00-roadmap]] hub + 13 sections from basics → DP), each with ASCII diagrams, worked examples, code, complexity, practice problems, and pitfalls. Cross-linked to seed notes ([[two-pointers]], [[binary-search]], [[hashing]], [[graphs]], [[heaps]]).
## [2026-05-29] ingest | Authored the Hashing track: 7 pages in dsa/hashing/ ([[hashing-00-roadmap]] hub + fundamentals, frequency map, set lookup, complement/index map, prefix+hashmap, strings & window). Removed the old combined arrays-and-hashing note and repointed its links to the new tracks. Reorganized dsa/ into per-topic folders (arrays/, hashing/).
## [2026-05-29] build | Viewer now shows sub-folders: build.py emits a `group` (the sub-folder name) and the sidebar renders collapsible sub-groups within a domain (DSA → Arrays / Hashing / Core Concepts). Domains without sub-folders stay flat. Added ?v= cache-buster to styles/app links.
## [2026-05-29] edit | Set all 14 Arrays + 7 Hashing pages to status: new (not started yet) — they were seeded as "learning". Regenerated notes-data.js.
## [2026-05-29] build | Moved Coding / DSA to the top of the sidebar (order 0). Added mobile support: build.py --serve serves the viewer over LAN + serve-mobile.bat + README "Run it on your phone" section. Bumped app.js cache-buster to v3.
## [2026-05-29] build | Made the viewer an installable offline PWA (manifest + icons + sw.js service worker) and added optional cross-device progress sync: app/sync.js moves status from localStorage to Firestore (Google sign-in), offline-capable, latest-write-wins, with graceful local-only fallback when unconfigured. Added firebase.json/.firebaserc/firestore.rules + MOBILE-SETUP.md. Verified local-only mode: SW controlling, no errors, status path works. Cache-busters: styles v3, app v4.
## [2026-05-29] build | Consolidated to a single run.bat: it now runs `build.py --serve --open` — rebuild + open on the laptop (localhost) + serve to the phone over LAN, in one click. build.py serve() gained open_browser + graceful "port busy" handling. Removed serve-mobile.bat.
## [2026-05-29] build | Added self-hosted sync option (user wants to host on their own WordPress server inkpioneers.in): server/sync.php (PHP+MySQL, last-write-wins, X-Sync-Key auth) + server/schema.sql + server/config.sample.php (config.php gitignored). sync.js rewritten to support 3 backends — self-hosted server (offline queue + poll/flush), Firebase, or local-only — all auto-detected with graceful fallback. app/sync-config.js holds just the endpoint URL (passphrase is server-checked, never in served files). SELF-HOSTED-SYNC.md documents cPanel deploy into a /wiki/ subfolder (strongly advised NOT to delete the blog; backup first). Verified local-only mode unchanged: mode=local, no errors, status path works.
## [2026-05-29] fix | Deployed on inkpioneers.in/wiki/. Host returned 403 for sync-config.js → moved endpoint inline into index.html. Then the sync button was missing ON MOBILE: it lived in the sidebar footer which sits behind the phone's bottom browser toolbar (100vh issue). Moved the "Sync" button to the TOP BAR (always visible) and set #sidebar/#main to 100dvh. Bumped styles v4 / app v5 / sw cache v4. Verified at 375px: Sync button at y=12, visible.
## [2026-05-29] fix | sync.php 500 = wrong DB_NAME in config.php (had the account prefix u242845725 instead of the full u242845725_3lbWa from wp-config.php). Corrected; sync working. Made sync.php report the real DB error on 500 for future debugging. Flattened the deploy bundle (no subfolders) + fixed zip perms (0644) after extraction issues.
## [2026-05-29] docs | Rewrote README.md as a portfolio-quality piece (matching the neurodesk-ai style): badges, one-paragraph pitch, Mermaid architecture diagram, features + tech-stack tables, quick start, structure, the LLM-wiki rationale, author links.
## [2026-05-29] build | Added one-command auto-deploy: deploy.py (rebuild + flat bundle + FTPS upload to /wiki/, never touches server config.php) + deploy.bat + deploy-config.sample.json (deploy-config.json gitignored) + DEPLOY.md. `python deploy.py --ls` helps find remote_dir. Replaces manual File Manager uploads.
## [2026-05-29] build | Fully automatic deploy on git push: .github/workflows/deploy.yml runs deploy.py with FTP creds from repo Secrets (FTP_HOST/FTP_USERNAME/FTP_PASSWORD, set via gh). deploy.py reads creds from env in CI. Verified FTPS + the real remote_dir (domains/inkpioneers.in/public_html/wiki, Hostinger layout) with a successful manual deploy (17 files, site 200).
## [2026-06-06] ingest | Built out the full DSA folder: dsa/README.md master roadmap + 11 new topic pages, each in its own folder (group via frontmatter): [[stack]], [[linked-list]], [[trees]], [[heap]], [[backtracking]], [[graphs]], [[dynamic-programming]], [[greedy]], [[intervals]], [[strings]], [[math-and-bits]]. Same template as the arrays/hashing tracks (when-to-use, idea, ASCII diagram, code, complexity, canonical NeetCode problems, pitfalls, related). Complements the existing Arrays (14) + Hashing (7) tracks → DSA now covers the full NeetCode-150 topic map. Updated index.md.
## [2026-06-06] fix | Reworked the DSA buildout per feedback (it had duplicates + alphabetical order + thin topics): (1) DEDUPE — deleted the seed "Core Concepts" DSA notes (removed content/coding.js) and the standalone greedy/ + dynamic-programming/ folders (they live in the Arrays track); repointed old seed wikilinks. (2) LEARNING ORDER — app.js GROUP_ORDER sorts DSA sub-groups Arrays→Hashing→Strings→Stack→Linked List→Trees→Heap→Backtracking→Graphs→Intervals→Math&Bits (app v6). (3) DEPTH — expanded the 9 new topics from single pages into full basics→advanced tracks (00 roadmap + numbered pages, 37 pages total). Verified: 58 DSA pages, 11 ordered tracks, zero DSA dead links, no console errors.
